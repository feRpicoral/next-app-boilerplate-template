// eslint-disable-next-line jest/no-export
export {};

/*
 * Dummy test to test the coverage report, since
 * using watchAll=true causes problems when generating
 * the report due to the way react-scripts is structured.
 *
 * This file has no purpose what so ever and should be deleted
 * as soon as the first real test is implemented.
 */

describe('Suite Dummy', () => {
    it('should be a dummy test', () => {
        expect(true).toBeTruthy();
    });
});
